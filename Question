? 브라우저별 엔진의 종류
? 각 엔진별 구성도
 ex. 아래처럼 다 다르게 구분되어있다. 그래서 엔진별 구성도, event loop, stack/heap/queue 동작 방식이 궁금
 비동기 호출을 위해 사용하는 setTimeout이나 XMLHttpRequest와 같은 함수들은 자바스크립트 엔진이 아닌 Web API 영역에 따로 정의되어 있다,
 이벤트 루프와 태스크 큐와같은 장치도 자바스크립트 엔진 외부에 구현되어있다
 node.js의 환경
